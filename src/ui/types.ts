/**
 * Types for webview UI
 *
 * Prefer importing protocol/transport types from this file in UI code.
 */

// UI uses `moduleResolution: bundler`, so `.js` specifiers are not required here.
export type * from '../protocol/types';
