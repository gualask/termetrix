/**
 * Types for webview UI
 *
 * Re-exports shared types from the single source of truth.
 */

export type {
	DirectoryInfo,
	ScanMetadata,
	ScanResult,
	LOCResult,
	ViewData,
	ProgressData,
	MessageFromExtension,
	MessageToExtension,
} from '../shared/types';
